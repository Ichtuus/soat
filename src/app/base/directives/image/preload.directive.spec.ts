import { PreloadImageDirective } from './preload.directive';

describe('ImageDirective', () => {
  it('should create an instance', () => {
    const directive = new PreloadImageDirective();
    expect(directive).toBeTruthy();
  });
});
